<div class="container">
    <h1>Your Moves</h1>
    <ul>
        <% @moves.each do |move| %>
            <li><%= move.moving_date %> to: <%= move.street_name %></li>
        <% end %>
    </ul>

</div>

