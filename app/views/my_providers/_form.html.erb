<%= simple_form_for [@my_provider] do |f| %>
  <%= f.input :identifier_value, label: "requires your #{@my_provider.provider.identifier_name}:" %>
  <hr>
  <%= f.submit "Save", class:"btn btn-success" %>
<% end %>